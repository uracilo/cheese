<%= form_for(user_product) do |f| %>
  <% if user_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_product.errors.count, "error") %> prohibited this user_product from being saved:</h2>

      <ul>
      <% user_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :productdescription %>
    <%= f.text_area :productdescription %>
  </div>

  <div class="field">
    <%= f.label :quantity %>
    <%= f.number_field :quantity %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.number_field :price %>
  </div>

  <div class="field">
    <%= f.label :weight %>
    <%= f.number_field :weight %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
